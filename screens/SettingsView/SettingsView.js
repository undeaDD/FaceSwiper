import { View } from 'react-native';

export const SettingsViewOptions = {
    title: "Einstellungen",
}

export default SettingsView = () => {
    return (
        <View style={{flex: 1}}>
        </View>
    );
}